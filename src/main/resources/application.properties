jdbc.host = jdbc:h2:file:~/src/main/resources/db/user_store.h2.db;
jdbc.user = h2
jdbc.password = h2
CREATE_TABLE = create table IF NOT EXISTS USERS (ID INT auto_increment, FIRSTNAME VARCHAR(30) not null, SECONDNAME VARCHAR(30) not null, SALARY DOUBLE not null, DATEOFBIRTH DATE not null, constraint USERS_PK primary key (ID)); create unique index IF NOT EXISTS USERS_ID_UINDEX on USERS (ID);
PATH_TO_RESOURCE = /static



